/**
 * This is the Shirt module.
 * @module shirt
 */

/** Try it on. */
exports.putOn = function(someShirt) {
}

/** Make it easier to put on and remove. */
exports.unbutton = function(someShirt) {
}

/**
 * A class within a module...
 *
 * @class
 */
this.Book = function(title) {
    /** The title of the book. */
    this.title = title;
}
